[pytest]
testpaths = tests
markers =
    manual: marks tests requiring manual verification (deselect with -m "not manual")
    anyio: anyio async marker (asyncio backend by default; used by tests/tui/textual)
addopts = -ra --strict-markers
filterwarnings =
    ignore::DeprecationWarning
